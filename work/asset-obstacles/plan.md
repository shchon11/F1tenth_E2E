Asset-only obstacle controls: preserve legacy untagged scenario loading, add explicit versioned asset suffix to fresh UI choices; use the existing six prop builders and dimensions as the render/physics/planning contract. Remove separate props category from both pickers, add asset/size controls, preserve arbitrary restored scenarios without rewriting history. New asset placement uses validated finite footprints and leaves wall grids unchanged. Regression checks cover grammar, deterministic placement, scaling, and real Qt controls.
