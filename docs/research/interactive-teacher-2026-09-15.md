# A teacher that races the other car (2026-09-15)

Branch `feat/interactive-teacher`, base main `0b78111`. Everything here is simulation.

## The question

Every DAgger run this project has ever done distilled `RacelineTeacher`: pure pursuit on a line
computed once from the occupancy grid, with a grip-aware speed profile and privileged latency
compensation. It has never heard of the other cars. In a race it is kept off them by one mechanism,
the env's follow-gap cap, which can only *slow it down* (`gym_env.follow_cap`).

The consequence is in the project memory as a flat statement — *DAgger cannot teach overtaking* —
and it is not a statement about DAgger. It is a statement about the teacher: a student can only be
taught what its teacher does, and this teacher's answer to another car is to lift. Static obstacles
it can teach, because the raceline is optimised on a grid that contains them. Dynamic behaviour has
had to come from PPO, where the reward has a contact penalty and, since the events work, an
overtake bonus — and where [failure-attribution](failure-attribution-2026-09-13.md) §5 measured
what actually happens: races end on walls (32 of 42 for the frozen policy), half of them hit while
alongside or just behind the opponent, and the contacts that do happen are side-by-side at
+1.5 m/s. *Lateral room during a pass* is the problem, and nothing in the training loop has ever
demonstrated one.

This note is the other end of that. It builds a teacher that itself uses the opponent's future, and
asks first whether that teacher out-races the raceline teacher — because a teacher that cannot use
the future cannot teach it.

## What the teacher is

`f1sim/interactive_teacher.py`. It keeps the raceline teacher as its reference and puts a small
best-response search on top of it.

**The candidates are in the student's own action space.** 5 lateral offsets (−0.6 … +0.6 m of the
raceline) × 4 speed multipliers (1.0, 0.8, 0.55, 0.3) = **20 plans in the existing 8-D plan space**
(`f1sim.mpc`: 6 curvature knots + 2 speed targets). Each offset candidate is generated by
`RacelineTeacher.plan_action` itself, on one (5 × B) tile, so a candidate *is* the raceline
teacher's plan through that offset line — the lane clamp, the understeer compensation, the latency
compensation and the Gauss-Newton fit are its, not a reimplementation that would drift away from it.

**Each candidate is rolled out by `mpc.reference`**, the function the plan tracker walks its own
reference with: the speed profile along the arc, the walk from the measured speed under the
acceleration bounds, the run-on past the end of a short plan. Twenty-one samples, 0.05 s apart, one
second ahead. Scoring a plan by re-integrating its curvature at the commanded speed instead would
score a manoeuvre the tracker cannot perform.

**Each is scored** by

    C = C_progress + 30 C_wall + 8 C_opp_future + 1 C_clear + 0.35 C_smooth

and the argmin is the label. `C_progress` is the arc the plan takes out of the raceline over the
horizon (endpoint projected onto a local window of the line and refined along its tangent), divided
by what the speed limit could have taken. `C_wall` is the mean depth the body would be inside a
wall, as a fraction of its half-width. `C_clear` is the mean squared shortfall of free space against
0.25 m — the same margin `--plan-clearance-penalty` uses, and the one
[failure-attribution](failure-attribution-2026-09-13.md) §2 found 85 % of collisions under.
`C_smooth` is how far the candidate's trajectory runs sideways from the reference plan's.
`C_opp_future` is the point, and has its own section.

### Three choices that were not obvious, and what decided them

**1. The opponent kernel is an ellipse, not a radius.** `C_opp_future` measures separation in the
candidate plan's own frame at that instant, with semi-axes (0.62, 0.34) m for the hard term and
(1.10, 0.50) m for the soft one — longitudinal first. The suite's own T-family note records that on
a 0.70 m half-lane a completed pass is rare; two cars side by side have at most ~0.6 m of
centre-to-centre room. A **circular** safety radius large enough to stop a rear-end — the bodies are
0.58 m long — is therefore also large enough to forbid every pass the track allows, and a teacher
built on one is a slower raceline teacher wearing a new name. The ellipses are the car.

**2. The offset family arrived carrying a speed cut, and had to be separated from it.**
`RacelineTeacher.plan_action(offset=x)` reads the car's lateral error *against the line it is
planning through*, and a car sitting on the raceline is by definition 0.6 m off a 0.6 m offset line.
Its off-line slowdown (`1 - 0.6 * lat_err`, floored at 0.3) then cut the commanded speed by up to
30 % for every offset candidate. Measured on one pose on `gen:competition:0`: offset 0 reached
+1.57 m of body-frame lateral travel over the horizon, offset −0.5 reached +0.30 and offset +0.5
reached +0.68 — not a monotone family at all, because the three were at three different speeds. So
"go left" arrived already carrying "and slow down", and the argmin could never separate the two.
The family now takes its speed from the on-line plan for every offset.

**3. `C_smooth` is a lateral departure, not a curvature-knot departure.** Written first as the mean
squared knot difference from the reference plan, it was worth 0.001 of the objective — a lateral
shift D over a plan of length L needs a knot amplitude of about 6D/L², so the same 0.3 m of line
change is worth forty times more at 2 m/s than at 8 m/s and no single weight is right at both ends.
And it has real work to do: with nothing at all to race, cutting to the inside of the raceline buys
about 0.01 of the progress term, because arc is scored against a line whose speed profile was
computed for *that line's* curvature and not for a shorter one. Without this term the argmin of an
empty road is a permanent 0.6 m inside line. Measured as the trajectory's lateral distance from the
reference trajectory, in units of 0.5 m, it is the same number at every speed.

## The opponent's future, and whether it is real

`F1VecEnv.car_future(times, model=)`. A batched simulator cannot step ahead and come back, so this
is a **prediction, not a peek** — each car's own controller carried forward, which the env already
knows this step:

* a **teacher-driven** car is walked along its raceline from where it is, at each grid point's own
  profile speed times the race's speed scale times **the scheduled event's multiplier, held for
  exactly as long as that event has left to run** and 1.0 after it expires, capped by the follow-gap
  speed if it is behind another car, accelerating and braking under bounds (braking at 3.0 m/s²,
  which is what a plan tracker measurably delivers, not its nominal 5.0). Its lateral offset is the
  one `OpponentEvents` is holding right now — a `shift`'s ramp, a `defend`'s block — held constant.
* a **policy-driven** car is read off the plan tracker's own predicted trajectory
  (`PlanTracker.last_pred`), put back into the world through the pose it was solved from and
  continued straight past its 0.6 s horizon.
* `constv` is world-frame constant velocity, and is the floor the other two have to beat.

Every prediction is anchored at the car in **position and heading**, and both errors are closed over
the tracker's own lookahead **distance** (2 m), not over a clock. Both details were found by
measurement rather than by taste: on a clock, a car that has just been told to stop is predicted to
slide half a metre sideways onto the line while standing still, and the braking event the label
exists to report is swamped by it; and without the heading term, 0.2 rad of heading error is 7.6 cm
of the 38 cm a car travels in 0.1 s — larger than the lateral motion being predicted.

### The label against the realised future

`python -m f1sim.learn.opp_future_check`. Every step, every car's predicted displacement over each
horizon is recorded with the pose it was predicted from; `h / control_dt` steps later it is compared
with where the car actually is. A sample is dropped when any car of that race ended its episode in
between — an opponent that crashes is respawned in place, and its position half a second later is
not the continuation of the motion anybody extrapolated. R² is on the **displacement in the car's
own body frame**, which is the quantity the label encodes; against raw world position every model
scores ~1.0 because the track is large.

PENDING-LABEL-TABLE

## Is it affordable?

The contract's budget is 3× `RacelineTeacher` per label, because the label is produced for every env
on every collection step. `work/bench/bench_teacher.py`, both teachers timed on the same env, the
same states, interleaved, with the opponent futures inside the interactive teacher's time:

PENDING-BENCH-TABLE

## Does the time-indexing decide anything?

Three synthetic scenes, built on a real raceline and a real distance field with the opponents'
futures supplied as data (`tests/test_interactive_teacher.py`). The point of each is that it is
invisible at t = 0.

| scene | what the teacher chose |
|---|---|
| opponent 2.6 m ahead on the line, drifting right at 0.55 m/s while falling back | **offset +0.3 at full speed** — the left of it |
| opponent 3.0 m ahead covering 0.4 m in the second the ego would cover 5 | **offset +0.6 at full speed** — a pass, not a lift; the best on-the-line option is the 0.3× speed scale |
| the same car moving *into* the left lane while a second holds the right | **speed < 1.0** — it backs off |
| no opponent in contention | **offset 0.0 at speed 1.0** — the raceline teacher's own plan, exactly |

The controlled comparison is the fourth row of the first scene. Scored a second time with the
opponents held frozen where they are *now* — same candidates, same walls, same progress, same
smoothness, so every term but one is identical — the opponent cost of the ±0.6 passes differs by
0.077 (they are the same move), and staying on the line costs 2.93. With the motion, the right-hand
pass costs 2.02 and the line costs 0.22: the car that is *leaving* has stopped being a reason to
move, and the side it is leaving *towards* has become one. A present-tense cost has neither number.

## The teacher's own ceiling

PENDING-D2

## Distilling it

PENDING-D3

## What this licenses, and what it does not

**Licensed.**

* *A teacher that scores candidate plans against the opponents' predicted positions makes the racing
  choice on the three situations a present-tense cost cannot see*, and the controlled comparison
  isolates the time-indexing: the same candidates, the same walls, the same progress and smoothness,
  with only the opponents' assumed motion changed.
* *Its prediction of where a car will be is better than constant velocity from a quarter of a second
  out, and on the lateral axis it is better in kind rather than in degree* — measured against the
  realised future on logged rollouts, with every sample that crossed a race boundary dropped.
* *It fits inside a DAgger collection loop.* 2.14x `RacelineTeacher` per label against a 3x budget.
* *Every flag off is the run it was.* `--teacher raceline` is the previous DAgger loop;
  `--opp-token off` produces the same observation dict, key for key; `plan_action` with no `idx` is
  what it was, one deduplicated projection aside (the same call, made once instead of twice).
* *The oracle cannot leave the simulator by accident.* Four independent refusals, each with its own
  reason: the exporter (an .onnx outlives its checkpoint), the ROS node (before it builds an
  observation), `ObsBuilder` (the deployment-side builder cannot build those columns at all), and
  the benchmark adapter (a suite score taken with it is not comparable with one taken without it).

**Not licensed.**

* **Any racing claim, in either direction, until Deliverable 2 has run.** Nothing above says the
  interactive teacher completes more races, passes more, or touches fewer cars than the raceline
  teacher; `work/gate.md` fixes what would settle that, and the measurement is GPU work.
* **Any claim that the five weights are right.** They were set from the measured scale of each term
  -- the corner-cut bonus the smoothness term has to price back, the lane width the opponent kernel
  has to fit a pass through -- and not tuned against a racing outcome. Nobody has swept them.
* **Anything about a student.** No policy in this branch has been distilled from this teacher.
* **Anything about the real car.** The teacher is privileged by construction: it reads the other
  cars' state out of the simulator, and so does every input it hands a student.
