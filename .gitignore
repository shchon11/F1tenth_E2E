# python / build
__pycache__/
.venv/
*.pyc
*.egg-info/
.pytest_cache/
build/
# training artefacts (runs live in ~/f1sim_runs)
wandb/
*.pt
# archived result policies, stripped of optimizer state (see checkpoints/README.md)
!checkpoints/*.pt
*.onnx
*.trt
*.log
# generated videos (re-render with f1sim.learn.watch --record / --highlights)
docs/*.mp4
!docs/promo.mp4

# Editor-local settings; the tree carries none and the toolchain reads none.
.vscode/
